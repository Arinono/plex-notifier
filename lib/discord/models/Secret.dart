class Secret {
  String join;
  String spectate;
  String match;

  Secret(Map secret) {
    join = secret['join'];
    ;
    spectate = secret['spectate'];
    ;
    match = secret['match'];
    ;
  }
}
